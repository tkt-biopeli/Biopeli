import Phaser from 'phaser'

export default class {
    constructor ({ game, x, y, type }) {
        this.game = game
        this.x = x
        this.y = y
        this.tileType = type
    }

    update () {

    }
}
